# /spec — alias de Agent Skills

Aplica el workflow canónico `.agent/workflows/spec.md` con estos argumentos:

$ARGUMENTS

No edites código en esta fase. Entrega una Spec aprobable y deja el siguiente
paso explícito: `/plan`.
