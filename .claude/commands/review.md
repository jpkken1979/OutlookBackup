# /review — alias de Agent Skills

Aplica `.agent/workflows/review.md` con estos argumentos:

$ARGUMENTS

Revisa correctitud, seguridad, arquitectura, tests y performance. El siguiente
paso es `/simplify` cuando haya mejoras seguras o `/ship` si el diff ya está
limpio.
