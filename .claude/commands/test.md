# /test — alias de Agent Skills

Aplica el workflow canónico `.agent/workflows/test.md` con estos argumentos:

$ARGUMENTS

Reporta evidencia de tests, types y lint. No declares éxito por la ausencia de
salida; entrega el resultado real de cada gate.
