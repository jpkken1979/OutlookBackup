# /plan — Planning commands

## Usage
- `/plan` — Show active plan status
- `/plan new <task>` — Create new plan from template
- `/plan recover` — Recover from interrupted session

## Description
Manages the 3-file planning system (task_plan.md, findings.md, progress.md).
Plans are stored in `.claude/planning/active/`.
