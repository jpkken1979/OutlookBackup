Invoca el skill `uiuxexpert` para buscar en shadcn/ui (https://ui.shadcn.com/) y en la web las mejores implementaciones UI/UX modernas e impresionantes.

Flujo:
1. Investigá en shadcn/ui si el componente/patrón solicitado existe usando context7 o WebFetch
2. Buscá alternativas modernas e impresionantes (aceternity-ui, origin-ui, magic-ui, tremor)
3. Presentá 3 opciones con código completo (simple → media → avanzada)
4. Recomendá cuál implementar para el stack actual (React 19 + Tailwind v4 + Framer Motion 12 + Tauri)
5. Si el usuario confirma → implementá directamente en el proyecto

Stack del proyecto: React 19, Tailwind CSS v4, Framer Motion 12, TypeScript strict, Lucide React, Tauri 2.
Paleta: cyan-400 primario, sin purple/indigo. Grid: p-4 o p-6 únicamente.
Variants de Framer Motion SIEMPRE fuera del componente.

Si no se especifica componente, presentá un menú de categorías:
- Feedback (toasts, alerts, banners, skeletons)
- Navigation (tabs, breadcrumbs, command palette, sidebar)
- Data display (tables, cards, bento grids, timelines, charts)
- Forms (inputs, selects, date pickers, file upload)
- Overlay (modals, drawers, tooltips, popovers)
- Animation (page transitions, scroll effects, micro-animations)

Argumento: $ARGUMENTS
