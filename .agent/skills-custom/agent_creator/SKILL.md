---
name: Agent Creator
description: Deprecated legacy agent scaffolding skill. Replaced by `D:\\Git\\OpenAntigravity\\.agent\\skills\\agent_creator\\SKILL.md` for the current `.agent/agents/<agent>/` model.
---

# Deprecated Agent Creator

This legacy copy is retained only as a migration breadcrumb.

## Status

- canonical replacement: `D:\Git\OpenAntigravity\.agent\skills\agent_creator\SKILL.md`
- repository model: `.agent/agents/<agent>/IDENTITY.md` + `.agent/agents/<agent>/SYSTEM_PROMPT.md`

## Why it is deprecated

This copy still points at the older backend-era scaffolding model and should not be used as the active workflow.

## What to do instead

Use the canonical skill under `.agent/skills/agent_creator/` for new agent creation or migration work.
