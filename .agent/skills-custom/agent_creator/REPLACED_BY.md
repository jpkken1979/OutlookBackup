# REPLACED BY

This legacy custom agent creator has been replaced by:

`D:\Git\OpenAntigravity\.agent\skills\agent_creator\SKILL.md`

## Why

The custom copy still tracked the backend-era `BaseAgent` workflow. The canonical skill now targets the repository's current `.agent/agents/<agent>/` directory model.
